---
description: 
alwaysApply: true
---

---
description: Auto-continue sessions with AskQuestion after task completion and propose next steps.
alwaysApply: true
---

# Session Continuation

## 核心规则
- 每次任务完成后（输出结论/总结、任务结果、文件变更或回答完成）**必须最后执行 `AskQuestion`**，除非用户明确要求结束会话。
- 若因缺少关键输入而中断，先 `AskQuestion` 获取信息，继续执行后再次调用。
- 禁止为了修bug而修bug 而引入fallback代码。

## 禁止场景
- 中间步骤不要中断：读写文件、搜索、构建、测试、已可独立决策时，**不要**调用 `AskQuestion`。

## 续航选项（每次）
- 选项总数 3-5 个，必须包含固定选项：**结束会话**。
- 结构建议：
  - 首选：基于上下文最自然的下一步（1个）
  - 附加：相关建议（1-3个）
  - 末项：结束会话
- 选项须具体、可执行，避免泛化（如“继续优化”）。

## 终止条件
- 仅当用户选择**结束会话**时才停止；否则按用户选项执行并进入下一轮续航。
